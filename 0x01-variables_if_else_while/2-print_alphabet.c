#include<stdio.h>

/**
 *  main - lowercase alphabets printing
 * 
 * Return: Always(successprinting)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar (c);

	putchar ('\n');
	return (0);
}
