#include <unistd.h>#

/**
 * _putchar – writes the character c to stdout for me
 * @c: The character to print
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately. 
 */

Int _putchar(char c)
{
	Return (write(1, &c, 1));
}

